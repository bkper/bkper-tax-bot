{
  "name": "tax-bot",
  "version": "2.1.4",
  "license": "MIT",
  "main": "index.js",
  "projectId": "bkper-tax-trigger",
  "functionName": "prod",
  "scripts": {
    "clean": "gts clean",
    "upgrade:bkper": "yarn upgrade bkper --latest",
    "upgrade:api": "yarn upgrade @bkper/bkper-api-types --latest",
    "bkper:sync": "bkper app -u",
    "build": "yarn clean && tsc && cp package.json dist/",
    "watch": "run-p watch:*",
    "watch:compile": "tsc -w --incremental",
    "watch:webhook": "node webhook",
    "watch:nodemon": "nodemon --watch ./dist/ --exec yarn functions:dev",
    "functions:dev": "functions-framework --source=dist/ --target=doPost --port=3001",
    "functions:enable": "gcloud services enable cloudfunctions.googleapis.com --project $npm_package_projectId && gcloud services enable cloudbuild.googleapis.com --project $npm_package_projectId",
    "functions:authorize": "gcloud alpha functions add-iam-policy-binding $npm_package_functionName --member=serviceAccount:bkper-hrd@appspot.gserviceaccount.com --role=roles/cloudfunctions.invoker --project $npm_package_projectId",
    "functions:open": "open \"https://console.cloud.google.com/functions/details/us-central1/$npm_package_functionName?organizationId=305881217394&project=$npm_package_projectId&tab=logs\"",
    "open": "yarn functions:open",
    "patch": "yarn version --patch",
    "minor": "yarn version --minor",
    "major": "yarn version --major",
    "deploy": "yarn build && yarn --cwd dist/ deploy:dist",
    "deploy:dist": "gcloud functions deploy $npm_package_functionName --runtime nodejs12 --trigger-http --entry-point doPost --max-instances 1 --memory 128MB --timeout 360 --project $npm_package_projectId",
    "preversion": "yarn deploy",
    "postversion": "git push && git push --tags && echo \"Successfully released version $npm_package_version!\""
  },
  "devDependencies": {
    "@bkper/bkper-api-types": "^3.7.5",
    "@types/express": "^4.17.10",
    "@types/node": "^14.11.2",
    "gts": "^3.0.3",
    "localtunnel": "^2.0.1",
    "nodemon": "^2.0.7",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^1.7.1",
    "bkper": "^1.7.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-http-context": "^1.2.4",
    "source-map-support": "^0.5.19"
  }
}
