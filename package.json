{
  "name": "bkper-bot",
  "version": "1.3.0",
  "scripts": {
    "login": "clasp login",
    "open": "clasp open",
    "pull": "clasp pull",
    "push": "clasp push",
    "watch": "yarn push --watch",
    "patch": "yarn version --patch",
    "minor": "yarn version --minor",
    "major": "yarn version --major",
    "preversion": "yarn push",
    "postversion": "clasp deploy --description $npm_package_version --deploymentId AKfycbxF0YB7SD6f5tfz2Fc5v05QfW7Y65hZr8itiWB2RmpFz-3PvOUn  && git push && git push --tags && echo \"Successfully released version $npm_package_version!\""
  },
  "devDependencies": {
    "@bkper/bkper-app-types": "^1.8.1",
    "@google/clasp": "^2.3.0"
  }
}
